mod attribute;
mod example;
mod tree;

use attribute::{*};
use example::{*};
use tree::{*};

fn main() {
    println!("Hello, world!");
}


// fn learn_decision_tree(examples: Vec<Example>,attributes: Vec<Attribute>,parent_examples: Vec<Example>) -> Tree{
//     Tree {root: Node{v: Alt::Yes,children: Vec::new()}}
// }